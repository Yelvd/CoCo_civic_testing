#include "../civic.h"
#include "../array.h"
#include "../coreio.h"

/* Testing fibs function */
export int main()
{    
    int[3, 2] matI;
    float[3, 2] matF;

    /* scan matrices */
    scanIntMat(matI);
    printNewlines(1);
    printIntMat(matI);
    printNewlines(1);

    scanFloatMat(matF);
    printNewlines(1);
    printFloatMat(matF);
    printNewlines(2);

    return 1;
}
